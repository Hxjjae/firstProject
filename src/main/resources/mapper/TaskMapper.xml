<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.firstproject.task.TaskMapper">
    <insert id="insTask">
        INSERT INTO task
        SET title = #{title}
        , description = #{description}
        , taskpoint = #{taskPoint}
    </insert>
    <update id="updTask">
        UPDATE task
        SET title = #{title}
        , time = current_timestamp()
        , description = #{description}
    </update>
</mapper>